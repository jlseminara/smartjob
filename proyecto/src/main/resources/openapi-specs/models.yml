openapi: 3.0.3
info:
  title: Users Backend
  description: OpenAPI spec for Users Backend Service.
  version: 1.0.0

paths: { }

components:

  schemas:

    TelephoneDTO:
      type: object
      required: [number, cityCode, countryCode]
      properties:
        number:
          type: string
        cityCode:
          type: string
        countryCode:
          type: string

    AddUserPayload:
      type: object
      required: [name, email, password, phones]
      properties:
        name:
          type: string
        email:
          type: string
        password:
          type: string
        phones:
          type: array
          items:
            $ref: '#/components/schemas/TelephoneDTO'

    CreatedUserDTO:
      type: object
      properties:
        id:
          type: string
        created:
          type: string
          format: date-time
        modified:
          type: string
          format: date-time
        lastLogin:
          type: string
          format: date-time
        token:
          type: string
        isActive:
          type: boolean

    ErrorResponse:
      type: object
      properties:
        mensaje:
          type: string

